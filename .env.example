# Dashboard authentication (required for /dashboard and saving portfolio)
# Generate a random string for DASHBOARD_SESSION_SECRET (e.g. openssl rand -base64 32)
DASHBOARD_USERNAME=admin
DASHBOARD_PASSWORD=your-secure-password
DASHBOARD_SESSION_SECRET=your-long-random-session-secret
